/** Что принимает сервер; svg среди них нет — это исполняемый документ */
export const imageTypes = ['image/png', 'image/jpeg', 'image/webp', 'image/gif'];

export const MAX_IMAGE_SIZE = 5 * 1024 * 1024;
