/**
 * Чей проект открыт в редакторе. Пользователь и проект пока по одному, но
 * страница и картинки уже лежат в папке проекта, а она — в папке пользователя.
 * Когда появятся настоящие пользователи, поменяется только источник этих id.
 */
export const DEFAULT_USER_ID = 'default';
export const DEFAULT_PROJECT_ID = 'default';
